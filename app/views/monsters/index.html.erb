<p id="notice"><%= notice %></p>


<div class="row card grey monster-types">
  <div class="card-content">
      <h2><b>Monsters</b></h2>
      <% @monsters.each do |monster| %>
        <%= render partial: "monsters/monster_card", locals: {monster: monster}  %>
    <% end %>
  </div>
</div>